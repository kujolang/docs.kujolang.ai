# Scoped runtime upgrade content audit

Date: September 5, 2026. Origin: https://docs.kujolang.ai. User authorized updating sites with native runtime upgrade content and applying the AI-search SEO skill. Scope is upgrade documentation and its discovery surfaces, not a full site redesign or traffic audit.

Baseline source: 989a77b7d739a11e7db4ebbc3d7d0a2ba34cbea0. Deployed gh-pages tree 27c0e18046e245c28a6589f8086a593a12246902. Full generated bytes are preserved under raw/baseline-build outside output, with SHA-256 inventory committed before final evidence. Baseline and after crawls use the frozen seo_audit.py. Earlier local builds with the workspace runtime were stopped after reproducing documented build problems; they are not passing verification. Docs uses a checksum-verified official macOS x64 v1.2.2 build runtime as a local override. Main site uses its existing pinned Pages workflow. No installed runtime, runtime release, DNS, hosting architecture, or crawler training policy was changed.

The generated HTML crawl checks titles, descriptions, H1s, canonicals, sitemaps, link graph, image attributes, and JSON-LD syntax. Existing schema is retained; validity alone does not establish rich-result eligibility. Upgrade claims were checked against kujolang/kujo docs/RUNTIME_UPGRADE.md and docs/CLI_MACHINE_READABLE_CONTRACTS.md. Live probes check changed routes and shared discovery files. Synthetic crawler User-Agent probes are not proof that authenticated bots pass edge rules. No external visibility or performance improvement is claimed.

Reproduction: python3 seo_audit.py --repo <repository> --output <generated-directory> --audit-dir <new-evidence-directory> --phase after --origin https://docs.kujolang.ai. Keep this audit immutable; use a new scope directory for future changes.
